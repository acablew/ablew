<% if @subject.errors.any? %>
  <div class="alert alert-danger">
    <p><%= pluralize(@subject.errors.count, "error") %>prevented if from being saved.</p>
    <ul>
      <% @subject.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>